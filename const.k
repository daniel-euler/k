const sql = "select * from table where name = ? and age = ?"
const sql = `name = $name and age = $age`
const sql = `name = $name and age = ${age+1}`
const pi  = 3.1415926
const int a = 1
const string b = "bbbbbb"
const string c = 'cccc'
const string daniel= "leinad"
const string d = `my name is ${ daniel } `
const foo = struct {
     [5]i32  ar,
     [6]i8   rr,
}
